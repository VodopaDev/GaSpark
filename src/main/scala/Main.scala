import rdd.DataEntry.GasType
import org.apache.spark.{SparkConf, SparkContext}
import rdd.RDDLoader
import rdd.RDDLoader._

object Main extends App{

  override def main(args: Array[String]): Unit = {
  }
}
